 <div class="tile image">
                        <div class="tile-content">
                             <a href='{{pathTo "photos" this}}' class="place-right">
        <img title="{{title}}" src="{{thumbURL}}"
            alt="Thumbnail of a photo with title: {{title}}"/>
    </a>
                        </div>
                    </div>